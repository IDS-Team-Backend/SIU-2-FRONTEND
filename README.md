# SIU-2-FRONTEND

A minimal frontend website built with Flask.

## Setup

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

## Run

```bash
python app.py
```

Open http://127.0.0.1:5000

## Test

```bash
python -m unittest discover -s tests -v
```
